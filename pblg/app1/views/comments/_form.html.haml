= form_for(@comment, :validate => true) do |f|
  = f.hidden_field :post_id
  %p
    = f.label(:title)
    %br/
    = f.text_field :title
  %p
    = f.label(:body, "Comment")
    %br/
    = f.text_area :body
  %p= f.submit "Admin Comment"

