<% @page_title = "Subject List" %>

<%= link_to("<< Back to Menu", "/admin", :class => 'back-link') %>

<%=
  render(
    :partial => "shared/list",
    :locals  => {
      :objects   => @subjects,
      :page      => 'subject',
      :new_link  => {:action => 'manage', :event => 'new'}
    }
  )
%>

